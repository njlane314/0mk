# Illustrative application contracts; see README.md for the required inputs and tools.

all <- report.pdf

case.tar.zst <- geometry.mesh parameters.toml @local
	./pack $^ --output $@

result.json <- case.tar.zst @large
	./solve --input $< --output $@

report.pdf <- result.json @local
	./report $< --output $@

publish! <- report.pdf @local
	./publish $<
